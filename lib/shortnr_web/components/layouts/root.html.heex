<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="Shortnr" suffix=" · Link Shortener">
      {assigns[:page_title]}
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-zinc-50 min-h-screen">
    <header class="bg-white border-b">
      <div class="max-w-5xl mx-auto flex items-center justify-between px-4 py-3">
        <a href={~p"/"} class="text-zinc-900 font-semibold">shortnr</a>
        <nav class="flex items-center gap-4 text-sm">
          <%= if @current_admin do %>
            <span class="text-zinc-700">{@current_admin.email}</span>
            <.link
              href={~p"/admins/log_out"}
              method="delete"
              class="rounded-md bg-zinc-900 text-white px-3 py-1.5 hover:bg-zinc-700"
            >
              Log out
            </.link>
          <% else %>
            <.link
              href={~p"/admins/log_in"}
              class="rounded-md bg-zinc-900 text-white px-3 py-1.5 hover:bg-zinc-700"
            >
              Log in
            </.link>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="max-w-5xl mx-auto px-4 py-6">
      <.flash_group flash={@flash} />
      {@inner_content}
    </main>
  </body>
</html>
